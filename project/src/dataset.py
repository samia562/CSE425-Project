# -*- coding: utf-8 -*-
"""Untitled1.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1qSEi2je1UO3AFtL0R1TqPBmQMZz78nDE
"""

import os
import torch
from torch.utils.data import Dataset
import librosa
import numpy as np

class AudioDataset(Dataset):
    def __init__(self, audio_files, lyrics, transform=None):
        self.audio_files = audio_files
        self.lyrics = lyrics
        self.transform = transform

    def __len__(self):
        return len(self.audio_files)

    def __getitem__(self, idx):
        audio_path = self.audio_files[idx]
        y, sr = librosa.load(audio_path, sr=None)
        if self.transform:
            y = self.transform(y)
        return y, self.lyrics[idx]